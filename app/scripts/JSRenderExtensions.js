window.jsrender.views.helpers({
        getFields: function( count ) {
            var fieldsArray = [];
            for (var i=0; i <= count; i++) {
                fieldsArray.push({});
            }

            return fieldsArray;
        }
    });
